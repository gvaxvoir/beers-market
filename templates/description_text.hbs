<button type="button" class="close" aria-label="Close">
    <span aria-hidden="true">&times;</span>
</button>
<div class="text">
    {{#each this}}
    <h2>{{name}}</h2>
    <p>{{tagline}}</p>
    <p>{{description}}</p>
    <ul>
        {{#each food_pairing}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    {{/each}}
</div>